import 'package:flutter/material.dart';

class InfoBox extends StatelessWidget {
  List<List<String>> data = [];
  InfoBox({super.key, required this.data});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
